name: Lint, test and push to Docker
on: push
jobs:
  lint-and-test:
    name: Lint and test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Install Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install ruff poetry>=2.2.0
          poetry install --with typing --with test -E embed-server -E render-playwright
      - name: Run Ruff check
        run: poetry run ruff check --output-format=github .
      - name: Run Ruff format
        run: poetry run ruff format --check .
      - name: Run mypy
        run: poetry run python3 -m mypy fxtumblr
      - name: Run test suite
        run: poetry run python3 -m pytest

  docker:
    name: Push Docker images to Docker Hub
    needs: lint-and-test
    if: github.ref_name == 'v2'
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      attestations: write
      id-token: write
    steps:
      - uses: actions/checkout@v6

      - name: Log in to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Build and push
        id: push
        uses: docker/bake-action@v7
        with:
          source: .
          files: docker/docker-bake.hcl
          push: true
